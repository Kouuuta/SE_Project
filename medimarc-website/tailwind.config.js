// tailwind.config.js
/** @type {import('tailwindcss').Config} */
export const content = [
  "./index.html", // Make sure to include the index.html file
  "./src/**/*.{js,ts,jsx,tsx}", // Ensure that all React files are scanned
];
export const theme = {
  extend: {},
};
export const plugins = [];
  